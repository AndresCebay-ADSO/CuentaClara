# CuentaClara — Frontend

Interfaz React. Conecta con el backend en `http://localhost:3000`.

## Setup

```bash
npm install
npm run dev
```

> Esta carpeta es manejada por el equipo de frontend.
> Crear el proyecto con: `npm create vite@latest . -- --template react`
